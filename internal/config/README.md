# Config

Configuration loading, validation, defaults, and compatibility boundaries. This
area will cover data sources, model providers, budgets, automation policies,
storage, notifications, and deployment identity without storing secrets in the
repository.
