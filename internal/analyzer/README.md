# Analyzer

Contains deterministic, repeatable analyzers that turn raw evidence into
structured findings. Analyzer output records the analyzer version, input
evidence, rule or calculation, result, severity, and limitations.

Open-ended LLM reasoning belongs in ClayHarness. Rootforge-specific adaptation
belongs in `internal/harness`, not here.
