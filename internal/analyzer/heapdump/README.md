# Heap dump analyzer

Adapter boundary for `heapdump/hprofx`, the existing standalone HPROF analyzer.
The first integration will translate its outputs into versioned Rootforge
findings without moving framework-specific reasoning into generic HPROF parsing.
