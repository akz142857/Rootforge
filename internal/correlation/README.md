# Correlation

Builds the runtime-to-code graph and unified timeline:

```text
Incident -> Node -> Workload -> Image -> Build -> Commit -> File / Symbol
```

Edges distinguish verified mappings from inferences and unknowns. Temporal
proximity alone is not treated as proof of causation.
