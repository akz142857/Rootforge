# Example configuration

Sanitized configuration examples will live here once the configuration contract
is defined. Examples must default to read-only investigation and deny write
actions unless an explicit policy enables them.
