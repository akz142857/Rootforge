# rootforge-retriever

Optional low-privilege evidence retriever deployed near workloads when no
central observability API is available. It will expose predefined, bounded,
read-only operations and will never host an LLM or arbitrary shell endpoint.
