# rootforged

Long-running Rootforge control plane. It will receive incident events, own the
Incident Controller, schedule the Harness Agent, persist Cases, enforce policy,
and dispatch notifications.
