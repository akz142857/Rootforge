# rootforge

Administrative CLI boundary for configuration checks, Case inspection, offline
imports, replays, and explicit operator actions. Normal investigations are
started by events received by `rootforged`, not by an interactive `ask` command.
